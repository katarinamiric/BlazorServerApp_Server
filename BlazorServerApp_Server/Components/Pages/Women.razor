@page "/women"
@using BlazorServerApp_Server.Services
@using BlazorServerApp_Server.Data.Model
@rendermode InteractiveServer

@inject ProductService ProductService

<PageTitle>Women's Collection</PageTitle>

<div class="page-container">
    <h1 class="page-title">Women's Collection</h1>
    <p class="page-subtitle">Discover our elegant and modern designs for women.</p>

    <div class="category-links">
        <a href="/women/shoes">Shoes</a>
        <a href="/women/bags">Bags</a>
        <a href="/women/glasses">Glasses</a>
    </div>

    <div class="product-grid">
        @if (_products == null)
        {
            <p><em>Loading...</em></p>
        }
        else
        {
            @foreach (var product in _products)
            {
                <ProductCard Product="@product" />
            }
        }
    </div>
</div>

@code {
    private List<Data.Model.Product>? _products;

    protected override async Task OnInitializedAsync()
    {
        _products = await ProductService.GetProductsAsync("women");
    }
}